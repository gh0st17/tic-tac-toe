name: Makefile

on: [push]

jobs:
  build_macos:
    name: macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      
      - shell: bash
      - name: Alias
        run: |
          rc=/tmp/rcfile
          echo 'shopt -s expand_aliases' > $rc
          echo 'alias llvm-ar=ar ' >> $rc

      - shell: bash
      - name: Make
        run: |
          source /tmp/rcfile
          make