name: CMake Build Matrix

on: [push]

jobs:
  build_macos:
    name: Release
    runs-on: macos-latest
    steps:
      - name: Build
        run: clang++ -std=c++17 *.cpp
        working-directory: ${{github.workspace}}/src
